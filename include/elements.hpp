#ifndef _ELEMENTS_
#define _ELEMENTS_

#include <tuple>
#include "matrix.h"
#include "const.hpp"
#include <cmath>

std::tuple<double, double, double, double, double, double, double> elements(Matrix y);

#endif